version: 2
models:
  - name: stg_orders
    description: stage orders is one unique order per row
    columns:
      - name: orderid
        description: The primary key for stage orders
      - name: shipmode
        description: '{{doc("shipmode")}}'